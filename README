perl-TimeDate
------------------------------------------
The date parser contained in this distribution is far superior to the 
yacc based parser, and a *lot* faster. The parser contained here will 
only parse absolute dates, if you want a date parser that can parse 
relative dates then take a look at the Time modules by David Muir on 
CPAN.

Runtime requirements:
  perl-5.10.1-2

Build requirements:
  cygport-0.9.80-1
  make-3.81-2

Canonical website:
  http://search.cpan.org/dist/TimeDate/

Canonical download:
  http://search.cpan.org/CPAN/authors/id/G/GB/GBARR/

-------------------------------------------

Build instructions:
  unpack perl-TimeDate-1.20-X-src.tar.bz2
    if you use setup to install this src package, it will be
	 unpacked under /usr/src automatically
  cd /usr/src
  cygport ./perl-TimeDate-1.20-X.cygport all

This will create:
  /usr/src/perl-TimeDate-1.20-X-src.tar.bz2
  /usr/src/perl-TimeDate-1.20-X.tar.bz2

-------------------------------------------

Port Notes:

----- version 1.20-1 -----
Initial release for Cygwin 1.7.


Cygwin port maintained by:
http://sourceware.org/cygwinports/
